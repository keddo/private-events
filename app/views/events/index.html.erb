
<section class="section">
<div class="container">
  <div class="columns">
  <% if user_signed_in? %>
    <%= render 'profile' %>
  <% else %>
   <%= render 'sidenav' %>
  <% end %>
   <%= render 'event_list' %>
   <%= render 'related_events' %>
  </div>
</div>
</section>